# AutoResearch — TinyStories / T4
# Install on GCP T4 instance (Deep Learning VM has torch+cuda pre-installed)
#   pip install -r requirements.txt
#
# Install locally (dashboard only — no GPU needed):
#   pip install gradio plotly google-cloud-firestore anthropic

# ── Core training (pre-installed on GCP Deep Learning VM) ────────────────────
# torch        — pre-installed with CUDA on Deep Learning VM
# numpy        — pre-installed

# ── Dataset ───────────────────────────────────────────────────────────────────
datasets>=2.18.0

# ── Agent ─────────────────────────────────────────────────────────────────────
anthropic>=0.25.0

# ── Firestore ─────────────────────────────────────────────────────────────────
google-cloud-firestore>=2.16.0

# ── Dashboard ─────────────────────────────────────────────────────────────────
gradio>=4.36.0
plotly>=5.22.0
