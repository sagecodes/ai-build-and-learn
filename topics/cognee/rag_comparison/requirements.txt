# Answer generation
anthropic>=0.49.0

# Local ONNX embeddings — shared across all three backends
fastembed>=0.8.0

# UI
gradio>=4.44.0

# Config
python-dotenv>=1.0.0

# ── Backend: Vector RAG ───────────────────────────────────────────────────────
psycopg2-binary>=2.9.0
pgvector>=0.3.0

# ── Backend: Graph RAG ────────────────────────────────────────────────────────
neo4j>=5.0.0

# ── Backend: Cognee ───────────────────────────────────────────────────────────
# Base install — no postgres extra needed (using local LanceDB storage)
cognee>=0.1.0
lancedb>=0.6.0
